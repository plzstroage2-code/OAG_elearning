# ICTC e-Learning Draw — อ่านก่อนทำงานต่อ

โปรเจกต์นี้อยู่ใน E_learning_draw เท่านั้น อย่าแก้โปรเจกต์ PDF Bot ที่อยู่ด้านนอก

1. อ่าน TASK_STATUS.md เพื่อดูขั้นตอนล่าสุดและสิ่งที่ยังไม่ผ่าน
2. อ่าน docs/IMPLEMENTATION_PLAN.md และ docs/DECISIONS.md
3. ตรวจไฟล์จริงและผล npm run typecheck / npm test ก่อนเชื่อข้อความว่าเสร็จ
4. ทำงานถัดไปทีละช่วง อัปเดต TASK_STATUS.md ก่อนและหลังช่วงนั้น
5. รัน npm run checkpoint หลังงานที่ตรวจแล้ว และก่อนจบแต่ละ turn

เมื่อโควตา AI หมด โค้ดไม่ได้หายไป ให้เปิดโฟลเดอร์เดิมและสั่ง:

> อ่าน START_HERE.md และ TASK_STATUS.md ใน E_learning_draw ตรวจสถานะจริงแล้วทำงานค้างต่อ อย่าเริ่มโปรเจกต์ใหม่ อย่ารีเซ็ตข้อมูล และอย่าอ้างว่าระบบจริงพร้อมก่อนตรวจครบ

ไม่มีการซื้อเครดิต รีเซ็ตโควตา สลับโมเดล หรือใช้ API ที่เสียเงินอัตโนมัติ งานในเครื่องเดินต่อโดยไม่ต้องเรียก AI เมื่อเว็บเปิดใช้งานแล้ว
